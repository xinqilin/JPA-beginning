
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jpa?characterEncoding=utf-8&useSSL=false&serverTimezone=CST
    username: root
    password: mysql

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081